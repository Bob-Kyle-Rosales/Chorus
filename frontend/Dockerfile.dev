FROM node:22-alpine

WORKDIR /app

RUN corepack enable pnpm

# Install deps from lockfile; source is mounted as a volume at runtime
COPY package.json pnpm-lock.yaml ./
RUN pnpm install --frozen-lockfile --ignore-scripts

CMD ["pnpm", "dev"]
